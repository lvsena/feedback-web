import React from "react";
import routerProvider from "@pankod/refine-react-router";
import { TitleProps } from "@pankod/refine";

const { Link } = routerProvider;

export const Title: React.FC<TitleProps> = ({ collapsed }) => <h1></h1>;
